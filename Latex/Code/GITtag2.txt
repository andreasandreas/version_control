$ git tag <tagNameA> <commit-hash>                   #lightweight tag on certain commit
$ git tag -a <tagNameB> -m "message" <commit-hash>   #annotead tag on certain commit

$ git push origin <tagName>
